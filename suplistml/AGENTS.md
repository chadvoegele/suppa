# Environment Setup
To use `suplistml` library, you first need to setup the environment. Source the `.env` file and use the virtual environment at `.venv`.

Example:
```
cd $SUPPA_REPO_ROOT/suplistml/
source .env && .venv/bin/python -c "import suplistml; ..."
```
